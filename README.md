# Fruit Ripeness Classifier 🍌

Proyek ini merupakan aplikasi web berbasis *deep learning* yang digunakan untuk mengklasifikasikan tingkat kematangan buah menggunakan model CNN (MobileNet).

## 🧠 Model
Model dilatih menggunakan TensorFlow pada dataset buah pisang.  

## 🚀 Menjalankan Secara Lokal
```bash
# Membuat virtual environment
python -m venv venv

# Mengaktifkan virtual environment
venv\Scripts\activate  # Untuk Windows
source venv/bin/activate  # Untuk Mac/Linux

# Menginstal dependensi
pip install -r requirements.txt

# Menjalankan aplikasi
python app.py
